package de.hartz.software.parannoying.app.small

class HashHelperTest {

/*
TODO: remove or move to security module
@Test
fun hashStringDoesWork() {
    val input = "Test123"
    val actual = HashHelper.hash(input)
    assertNotEquals(input, actual)
}

@Test
fun hashTwoStringsDoesWork() {
    val input = "Test123"
    val input2 = "Test123"
    val actual = HashHelper.hash(input, input2)
    assertFalse(actual.contains(input))
}

@Test
fun hashTwoStringsDoesNotDependOnOrder() {
    val input = "Test123"
    val input2 = "Xyz987"
    val actual = HashHelper.hash(input, input2)
    val actualReversed = HashHelper.hash(input2, input)
    assertEquals(actual, actualReversed)
}

@Test
fun hashThreeStringsDoesNotDependOnOrder() {
    val input = "Test123"
    val input2 = "Xyz987"
    val input3 = "x"
    val actual = HashHelper.hash(input, input2, input3)
    val actualReversed = HashHelper.hash(input3, input2, input)
    assertEquals(actual, actualReversed)
}

@Test
fun hashStringWithSpecificLengthDoesWork() {
    val length = 4
    val input = "Test123"
    val actual = HashHelper.hashWithSpecificLength(input, length = length)
    assertNotEquals(input.substring(0, length), actual)
}

@Test
fun hashStringWithSpecificLengthDoesNotDependOnOrder() {
    val length = 4
    val input = "Test123"
    val input2 = "Xyz987"
    val actual = HashHelper.hashWithSpecificLength(input, input2, length = length)
    val actualReversed = HashHelper.hashWithSpecificLength(input2, input, length = length)
    assertEquals(actual, actualReversed)
}

@Test
fun hashStringWithSpecificLengthDoesNotDependOnOrderWithExampleData() {
    val length = DataSecurityHelper.IV_SIZE
    val input = "AAAAAAAAAAAAAAAA"
    val input2 = "ZOm6=7sKCqPrZzwQ"
    val actual = HashHelper.hashWithSpecificLength(input, input2, length = length)
    val actualReversed = HashHelper.hashWithSpecificLength(input2, input, length = length)
    assertEquals(actual, actualReversed)
}
*/
}
