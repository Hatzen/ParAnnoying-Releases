package de.hartz.software.parannoying.app.small

class StringSaltTest {

    /*
    TODO: remove or move to security module
    @Test
    fun hashStringDoesWork() {
        val input = DataSecurityHelper().intToString(5) + "Test123"
        var actual = DataSecurityHelper.saltString(input)
        assertNotEquals(input, actual)
        actual = DataSecurityHelper.removeSaltFromString(actual)
        assertEquals(input, actual)
    }
     */
}
